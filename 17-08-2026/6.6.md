# what is type annotation

In Python, type annotations (also called type hints) are used to explicitly declare the expected data types of variables, function arguments, and return values. Because Python is a dynamically typed language, the Python interpreter itself completely ignores these hints at runtime and does not enforce them; however, they are absolutely crucial for early bug detection, IDE autocomplete, and code maintainability